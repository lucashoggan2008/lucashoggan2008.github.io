{
  "name": "pages-test",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://lucashoggan2008.github.io",
  "dependencies": {
    "@react-three/fiber": "^7.0.26",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.2.1",
    "react-scripts": "5.0.0",
    "three": "^0.137.5",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "npm uninstall eslint && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build",
    "update": "git add . && git commit -m \"commit\" && git push origin master",
    "update-f": "npm run update && npm run deploy"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
