import React from 'react';
import '../css/home.css'
import {FullContainer, HalfContainer, ContainerDivider, ContainerRow, ThirdContainer} from '../components/Containers'
import {TitleText} from '../components/Text'
import PageBody from '../components/PageBody'

const Home = () => {
  return (
  <PageBody>

    <FullContainer>
      <TitleText>Home</TitleText>
      <ContainerDivider />
    </FullContainer>

    
  </PageBody>);
};

export default Home;
